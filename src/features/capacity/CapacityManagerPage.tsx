import { Card } from '@/components/Card';

// Admin monthly capacity manager — set/adjust slots per product type.
// Placeholder only.
export function CapacityManagerPage() {
  return (
    <div>
      <h1 className="mb-6 font-display text-3xl">Capacity</h1>
      <Card>
        <p className="font-sans text-sm text-charcoal-light">
          Capacity manager placeholder — configure monthly slots by product
          type and adjust mid-month.
        </p>
      </Card>
    </div>
  );
}
